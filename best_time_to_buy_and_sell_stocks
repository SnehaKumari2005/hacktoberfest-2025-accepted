class Solution {
public:
    int maxProfit(vector<int>& nums) {
       int mini=INT_MAX;
       int maxx=INT_MIN;
       for(int i=0;i<nums.size();i++){
        mini=min(mini,nums[i]);
        maxx=max(maxx,nums[i]-mini);
       }
       return maxx;
    }
};
